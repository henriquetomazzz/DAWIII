'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Você pode customizar a cor aqui
    },
  },
});

export default theme;